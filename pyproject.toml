[build-system]
requires = ["hatchling>=1.26,<1.28"]
build-backend = "hatchling.build"

[project]
name = "codex-hermes-a2a-bridge"
version = "0.2.0"
description = "Local MCP-to-A2A bridge from Codex to Hermes Agent"
readme = "README.md"
requires-python = ">=3.11,<3.12"
license = "Apache-2.0"
license-files = ["LICENSE"]
authors = [{name = "Codex Hermes A2A Bridge contributors"}]
keywords = ["a2a", "codex", "hermes-agent", "mcp"]
classifiers = [
  "Development Status :: 3 - Alpha",
  "License :: OSI Approved :: Apache Software License",
  "Operating System :: OS Independent",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.11",
  "Topic :: Software Development :: Libraries :: Application Frameworks",
]
dependencies = [
  "httpx>=0.27,<1",
  "mcp>=2.0,<3",
  "pydantic>=2.10,<3",
]

[project.optional-dependencies]
dev = [
  "pytest>=8,<9",
  "pytest-asyncio>=0.25,<2",
  "pytest-cov>=6,<8",
  "build>=1.2,<2",
  "mypy>=1.14,<2",
  "ruff>=0.9,<1",
  "twine>=6,<7",
]

[project.scripts]
codex-hermes-a2a-bridge = "codex_hermes_a2a_bridge.cli:main"

[tool.hatch.build.targets.wheel]
packages = ["src/codex_hermes_a2a_bridge"]

[tool.hatch.build.targets.sdist]
include = [
  "/CHANGELOG.md",
  "/CONTRIBUTING.md",
  "/.editorconfig",
  "/.env.example",
  "/LICENSE",
  "/README.md",
  "/SECURITY.md",
  "/docs",
  "/pyproject.toml",
  "/scripts",
  "/src",
  "/tests",
]
exclude = [
  "/.github",
  "/.venv",
  "/dist",
  "**/.DS_Store",
  "**/__pycache__",
  "**/*.pyc",
  "**/*.sqlite*",
  "**/*.log",
]

[tool.pytest.ini_options]
addopts = "-q --strict-markers"
asyncio_mode = "auto"
testpaths = ["tests"]

[tool.coverage.run]
branch = true
source = ["codex_hermes_a2a_bridge"]

[tool.coverage.report]
fail_under = 65
show_missing = true
skip_covered = true

[tool.ruff]
target-version = "py311"
line-length = 120

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM"]

[tool.ruff.lint.per-file-ignores]
"tests/*" = ["B017"]

[tool.mypy]
python_version = "3.11"
check_untyped_defs = true
disallow_untyped_defs = true
no_implicit_optional = true
warn_redundant_casts = true
warn_unused_ignores = true
warn_unreachable = true
